#!/usr/bin/env bash
#
#	"Node comes with npm installed so you should have a version of npm.
#    However, npm gets updated more frequently than Node does, so 
#    you'll want to make sure it's the latest version."
#
# 	See https://docs.npmjs.com/getting-started/installing-node
#

echo Updating npm...
sudo npm install npm -g
